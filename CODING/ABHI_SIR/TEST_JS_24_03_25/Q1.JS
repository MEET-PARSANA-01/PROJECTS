//Q1.FIND THE LAST ELEMENT OF THE ARRAY(FUNCTION).

let Billionare = [1, 2, 3, 4, 5, 6];

function findLastElement(Billionare) {
  return Billionare[Billionare.length - 1];
}

console.log(findLastElement(Billionare));
