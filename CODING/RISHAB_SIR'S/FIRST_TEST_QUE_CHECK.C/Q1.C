// Which condition correctly checks if a number n is prime in a function ?
#include <stdio.h>
int main()
{
  int n;
  printf("ENTER THE NUMBER: ");
  scanf("%d", &n);

  // if (n % i == 0 && i != 1 && i != n) ??
  {
    printf("\nTHE ENTERED NUMBER IS A PRIME NUMBER\n");
  }
  // else
  // {
  //   printf("\nTHE ENTERED NUMBER IS NOT A PRIME NUMBER\n");
  // }

  return 0;
}